# Core analysis
numpy==2.4.6
pandas==3.0.3
scipy==1.17.1
matplotlib==3.10.9

# Notebook execution (VS Code needs ipykernel;
# nbconvert only for headless runs)
ipykernel==7.2.0
nbconvert

# Interactive explorer app (app/ directory).
# requests is used directly by app/explain.py (Gemini REST).
streamlit==1.58.0
requests==2.32.5
